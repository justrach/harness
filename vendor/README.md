# Vendored dependencies

GPUI and gpui-base are vendored here so the app does not pull a GPL crate graph.
Both trees are Apache-2.0. License texts sit next to the crate sources
(`LICENSE-APACHE`). Attribution is recorded in `THIRD_PARTY_NOTICES.md`.
